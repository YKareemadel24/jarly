# AGENTS.md

Guidance for all coding agents working in this repo lives in [CLAUDE.md](./CLAUDE.md) — read it first. It covers commands (pnpm only), the domain layering (`lib/savings-core.ts` pure logic vs store/UI), hard invariants (integer minor units, milestones never roll back), and where feature code goes (not in `*_core/` directories).
